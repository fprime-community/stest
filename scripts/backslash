#!/bin/sh -e

# ----------------------------------------------------------------------
# backslash
# ----------------------------------------------------------------------
# Since backslashes are tricky and non-portable with echo,
# handle backslashes as follows:
# \$$ --> \
# \$\([^!]\) -> @\1
# \$! --> $
# ----------------------------------------------------------------------

sed -e 's/\$$/\\/g' -e 's/\$\([^!]\)/\\\1/g' -e 's/\$!/$/g'
